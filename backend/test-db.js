require('dotenv').config(); const { Pool } = require('pg'); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); const queries = ['ALTER TABLE pending_requests ADD COLUMN IF NOT EXISTS sku VARCHAR(100)', 'ALTER TABLE pending_requests ADD COLUMN IF NOT EXISTS category VARCHAR(100)', 'ALTER TABLE pending_requests ADD COLUMN IF NOT EXISTS price VARCHAR(100)', 'ALTER TABLE pending_requests ADD COLUMN IF NOT EXISTS reg_no VARCHAR(100)']; Promise.all(queries.map(q => pool.query(q))).then(() => console.log('Columns added successfully')).catch(err => console.error('DB Error:', err.message)).finally(() => pool.end());
